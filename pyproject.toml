[project]
name = "storage-ui"
version = "0.1.0"
description = "storage-ui Flask application"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=26.0.0",
    "requests>=2.32.0",
]

[dependency-groups]
dev = ["pytest>=8.0.0", "responses>=0.25.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
